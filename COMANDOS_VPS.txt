╔══════════════════════════════════════════════════════════════════════════════╗
║                                                                              ║
║                    🔍 COMANDOS PARA VERIFICAR A VPS 🔍                       ║
║                           IP: 49.13.1.177                                    ║
║                                                                              ║
╚══════════════════════════════════════════════════════════════════════════════╝

┌──────────────────────────────────────────────────────────────────────────────┐
│  ⚡ OPÇÃO 1: VERIFICAÇÃO AUTOMÁTICA (RECOMENDADO)                           │
└──────────────────────────────────────────────────────────────────────────────┘

  Execute no seu terminal local:

  bash scripts/verificar_vps.sh

  ⬇️ Isso irá:
  • Conectar na VPS via SSH
  • Verificar TUDO automaticamente
  • Mostrar relatório completo


┌──────────────────────────────────────────────────────────────────────────────┐
│  🖥️ OPÇÃO 2: COMANDOS MANUAIS                                               │
└──────────────────────────────────────────────────────────────────────────────┘

  1️⃣ CONECTAR NA VPS
  ─────────────────────────────────────────────────────────────────────────────

  ssh root@49.13.1.177


  2️⃣ VERIFICAÇÃO RÁPIDA (COPIE E COLE TUDO)
  ─────────────────────────────────────────────────────────────────────────────

  echo "════════════════════════════════════════════════════════════"
  echo "🔍 VERIFICAÇÃO RÁPIDA DO BOT"
  echo "════════════════════════════════════════════════════════════"
  echo ""
  echo "📁 Repositório:"
  ls -la /root/MRROBOT-FUTURE && echo "✅ Encontrado" || echo "❌ NÃO encontrado"
  echo ""
  echo "📄 Arquivo .env:"
  test -f /root/MRROBOT-FUTURE/.env && echo "✅ Existe" || echo "❌ NÃO existe"
  echo ""
  echo "🐍 Python:"
  python3 --version
  echo ""
  echo "📦 Ambiente virtual:"
  test -d /root/MRROBOT-FUTURE/venv && echo "✅ Existe" || echo "❌ NÃO existe"
  echo ""
  echo "🐳 Docker:"
  docker --version 2>/dev/null || echo "❌ Docker não instalado"
  echo ""
  echo "🤖 Bot rodando (systemd):"
  systemctl is-active scalping-bot && echo "✅ Rodando" || echo "❌ Parado/não configurado"
  echo ""
  echo "🐳 Bot rodando (docker):"
  docker ps | grep -q robot && echo "✅ Rodando" || echo "❌ Não rodando"
  echo ""
  echo "🌐 API respondendo:"
  curl -s http://localhost:8000/health >/dev/null 2>&1 && echo "✅ Funcionando" || echo "❌ Não responde"
  echo ""
  echo "════════════════════════════════════════════════════════════"


  3️⃣ SE O BOT NÃO ESTIVER RODANDO - INICIAR
  ─────────────────────────────────────────────────────────────────────────────

  # Opção A: Docker (RECOMENDADO)
  cd /root/MRROBOT-FUTURE
  docker-compose up -d
  docker-compose logs -f

  # Opção B: Systemd
  sudo systemctl start scalping-bot
  sudo systemctl status scalping-bot
  sudo journalctl -u scalping-bot -f


  4️⃣ VER LOGS EM TEMPO REAL
  ─────────────────────────────────────────────────────────────────────────────

  # Docker
  docker-compose logs -f

  # Systemd
  sudo journalctl -u scalping-bot -f

  # Arquivo
  tail -f /root/MRROBOT-FUTURE/logs/scalping_bot.log


  5️⃣ TESTAR API
  ─────────────────────────────────────────────────────────────────────────────

  # Health check
  curl http://localhost:8000/health

  # Estatísticas
  curl http://localhost:8000/stats

  # Trades abertos
  curl http://localhost:8000/trades/open

  # Trade manual de teste
  curl -X POST http://localhost:8000/trade/manual \
    -H "Content-Type: application/json" \
    -d '{"symbol": "BTCUSDT"}'


  6️⃣ VERIFICAR TELEGRAM
  ─────────────────────────────────────────────────────────────────────────────

  cat /root/MRROBOT-FUTURE/.env | grep TELEGRAM

  # Deve mostrar:
  # TELEGRAM_BOT_TOKEN=...
  # TELEGRAM_CHAT_ID=...


  7️⃣ VERIFICAR ÚLTIMA ATUALIZAÇÃO (GIT)
  ─────────────────────────────────────────────────────────────────────────────

  cd /root/MRROBOT-FUTURE
  git log -1
  git status


  8️⃣ REINICIAR BOT
  ─────────────────────────────────────────────────────────────────────────────

  # Docker
  docker-compose restart

  # Systemd
  sudo systemctl restart scalping-bot


  9️⃣ ATUALIZAR CÓDIGO (PULL DO GITHUB)
  ─────────────────────────────────────────────────────────────────────────────

  cd /root/MRROBOT-FUTURE
  git pull

  # Se usar Docker
  docker-compose build
  docker-compose up -d

  # Se usar systemd
  source venv/bin/activate
  pip install -r requirements.txt
  sudo systemctl restart scalping-bot


  🔟 MONITORAMENTO CONTÍNUO
  ─────────────────────────────────────────────────────────────────────────────

  # Criar script de monitoramento
  cat > /root/monitor.sh << 'EOFMON'
  #!/bin/bash
  while true; do
    clear
    echo "══════════════════════════════════════════════════════"
    echo "🤖 BOT SCALPING - MONITOR"
    echo "══════════════════════════════════════════════════════"
    echo ""
    echo "🏥 Status: $(curl -s http://localhost:8000/health | jq -r .status 2>/dev/null || echo 'OFFLINE')"
    echo "🎭 Modo: $(curl -s http://localhost:8000/health | jq -r .mode 2>/dev/null || echo 'N/A')"
    echo "📊 Trades: $(curl -s http://localhost:8000/trades/open | jq -r .count 2>/dev/null || echo '0')"
    echo "💰 PnL Hoje: $$(curl -s http://localhost:8000/stats?days=1 | jq -r .statistics.total_pnl 2>/dev/null || echo '0')"
    echo "📈 Win Rate: $(curl -s http://localhost:8000/stats?days=1 | jq -r .statistics.win_rate 2>/dev/null || echo '0')%"
    echo ""
    echo "══════════════════════════════════════════════════════"
    echo "Atualizando em 10s... (Ctrl+C para sair)"
    sleep 10
  done
  EOFMON

  chmod +x /root/monitor.sh
  /root/monitor.sh


┌──────────────────────────────────────────────────────────────────────────────┐
│  🚀 SETUP INICIAL (SE AINDA NÃO FEZ)                                         │
└──────────────────────────────────────────────────────────────────────────────┘

  # 1. Clonar repositório (se não existe)
  cd /root
  git clone https://github.com/seu-usuario/MRROBOT-FUTURE.git

  # 2. Configurar .env
  cd /root/MRROBOT-FUTURE
  cp env.template .env
  nano .env
  # Configure suas keys

  # 3. Opção A: Docker (RECOMENDADO)
  docker-compose up -d

  # 3. Opção B: Systemd
  python3 -m venv venv
  source venv/bin/activate
  pip install -r requirements.txt
  sudo cp systemd/scalping-bot.service /etc/systemd/system/
  sudo systemctl daemon-reload
  sudo systemctl enable scalping-bot
  sudo systemctl start scalping-bot


┌──────────────────────────────────────────────────────────────────────────────┐
│  📚 DOCUMENTAÇÃO COMPLETA                                                    │
└──────────────────────────────────────────────────────────────────────────────┘

  📖 VERIFICAR_VPS.md          - Guia completo de verificação
  🔧 docs/VPS_SETUP.md         - Setup inicial da VPS
  🐳 DOCKER_QUICKSTART.md      - Guia rápido Docker
  📱 TELEGRAM_QUICKSTART.md    - Configurar notificações


┌──────────────────────────────────────────────────────────────────────────────┐
│  🆘 TROUBLESHOOTING RÁPIDO                                                   │
└──────────────────────────────────────────────────────────────────────────────┘

  ❌ Bot não inicia:
     sudo journalctl -u scalping-bot -n 50

  ❌ API não responde:
     sudo ufw allow 8000
     sudo systemctl restart scalping-bot

  ❌ Erro de módulo:
     source venv/bin/activate
     pip install -r requirements.txt --upgrade

  ❌ Porta ocupada:
     sudo lsof -i :8000
     sudo kill -9 <PID>


╔══════════════════════════════════════════════════════════════════════════════╗
║                                                                              ║
║                  🎯 COMANDO MAIS IMPORTANTE DO DIA:                          ║
║                                                                              ║
║                    ssh root@49.13.1.177                                      ║
║                                                                              ║
║           Depois execute o comando de "VERIFICAÇÃO RÁPIDA" acima!           ║
║                                                                              ║
╚══════════════════════════════════════════════════════════════════════════════╝
